<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>The Palindrome Results!</title>
    <link rel="stylesheet" href="/site.css">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    {{!-- It's at root because we didn't specify a virtual directory when we registered the static file serving middleware --}}
</head>
<body>
    <div class="content">
        <h1>The Palindrome Results!</h1>
        <main>
            <span>Your input: </span><p {{#if isPalindrome}}class="success"{{else}}class="failure"{{/if}}>
                {{text-to-test}}
            </p>
            <p>
                {{#if isPalindrome}}
                    This is a palindrome!
                {{else}}
                    This is not a palindrome...
                {{/if}}
            </p>
        </main>

        <a href="/">Try another palindrome</a>
    </div>
</body>
</html>